package Engine.modules.ashley.components;

/**
 * Entities that contain a texture or sprite that's supposed to be rendered, will
 * be rendered if they (the entity) contain this component.
 */
public class RenderableComponent implements PooledComponent{
	
	@Override
	public void reset() {
		
	}
	
}
